/*import "@fastify/jwt";

declare module "@fastify/jwt" {
  interface FastifyJWT {
    JwtPayload: {
      id: string;
      name?: string;
      passport?: string;
      iat: number;
      decodedToken: string;
    }
    user: {
      id: string;
      name?: string;
      passport?: string;
      iat: number;
      decodedToken: string;
    }
  }
}*/